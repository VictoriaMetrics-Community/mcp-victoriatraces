apiVersion: v1
kind: Service
metadata:
  name: {{ include "mcp-victoriatraces.fullname" . }}
  labels: {{ include "mcp-victoriatraces.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector: {{ include "mcp-victoriatraces.selectorLabels" . | nindent 4 }}
